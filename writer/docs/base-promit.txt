本项目是由html、css、js来实现的，每个子目录都是一个独立的页面，尽可能做到让它不以来父目录及其他项目的代码（如果有js，就下载到本地）

工作时将整体工作拆分成若干可验证的子任务，整体工作先拆分成一份计划文档，确保文档中包含了所有开发的基础要求
## 每个子任务的工作流
1. 实现子功能
2. 如果是基础单元，添加测试代码到测试页面
3. 运行测试
4. 确认全部测试通过
5. 更新计划文档中对应任务的 checkbox
6. git commit


目前项目所在的目录是writer目录，其中docs里已经包含了所需要的文档。这个任务同样需要先更新文档，然后再继续工作。有一个服务基于项目的根目录提供了https访问

# AI 日志清理的功能没有效果
# 章节开头和章节结尾都有一个特殊的变量，变量名为 章节位置 ，如果是章节开头，那么其值就是开头，如果是结尾，其值就是结尾，如果是结尾，否则就是中间
  - *生成章节* 的名字改为 *生成并结束本章* 这会为章节位置变量的值设置为结尾。（章节开头就是如果原来章内容是空的，就是章节开头）

# 章节的最后一段允许删除或者修改
  - 在底部菜单栏增加一个重新生成，用来重新生成最后一段内容
  - 当点击最后一个段落的时候，直接将其内容复制到情节概述里，*开始生成* 按钮变成 *更新最后一段* ， 情节概述的标题后面 增加一个提示，点击右侧的x号可以取消更新。 情节概述的标题栏右边增加一个X号，点击后可以清除情节概述的内容，并且把 *更新最后一段* 的按钮改回 *开始生成*

# 提示词优化
  - 里面每一个步骤都对应一个高级AI，在选中第一个高级AI后，且下面两个高级AI是空的，自动将下面两个设置为该AI。
  - 生成优化后的提示词时，增加一个要求，引用变量时要注明引用的变量是什么，因为变量是直接替换的，如果不注明，会导致语义混乱

# 提示词优化页面 改动
  - 日志选择放在左边栏
  - 选择后在右侧展示该日志对应的信息
  - 此外提示词优化也分别给两个模型的输出内容打分，按照十分制
